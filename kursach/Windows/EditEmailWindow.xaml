<Window x:Class="kursach.Windows.EditEmailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kursach.Windows"
        mc:Ignorable="d"
        Title="Изменение email" 
        Height="300" 
        Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <StackPanel>
            <TextBlock Text="Изменение email" 
                       FontSize="18" 
                       FontWeight="SemiBold"
                       Margin="0,0,0,20"/>

            <Label Content="Текущий email"/>
            <TextBlock x:Name="CurrentEmailText" Margin="0,0,0,10"/>

            <Label Content="Новый email"/>
            <TextBox x:Name="NewEmailTextBox" Margin="0,0,0,10"/>

            <Label Content="Подтвердите пароль"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,20"/>

            <Button Content="Сохранить" 
                    Height="40"
                    Background="#FF4f4843"
                    Foreground="White"
                    FontWeight="SemiBold"
                    Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>